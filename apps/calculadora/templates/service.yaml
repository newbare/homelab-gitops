apiVersion: v1
kind: Service
metadata:
  name: calculadora
  namespace: {{ .Values.namespace }}
  labels:
    app.kubernetes.io/name: calculadora
    app.kubernetes.io/part-of: resilience-cloud
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: calculadora
  ports:
    - name: http
      port: 80
      targetPort: http
